extends layout
block content
    .main-content 
    h1 All furnitures 
    table(border=1)
        for item, index  in data 
            tr  
                td  #{index + 1}
                td 
                    a(href='/single-furniture/' + item.furniture_id) #{item.furniture_name}