extends layout
block content
    .main-content 
    h1 All category 
    table(border=1)
        for item, index  in data 
            tr  
                td  #{index + 1}
                td 
                     #{item.category_name}